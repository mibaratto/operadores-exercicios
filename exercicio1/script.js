

let respostaUsuario = prompt("Insira um número")
let restoBolleano = Number(respostaUsuario)%2 === 0
let resto = Number(respostaUsuario)%2
console.log("resto:", resto, "é par?:", restoBolleano)

// sim, o padrão é sempre zero
// quando o número é impar o resultado é sempre 1
